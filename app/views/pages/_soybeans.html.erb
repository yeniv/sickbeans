<div class="container-soybean">
  <% @soybeans.each do |soybean| %>
    <div class="soybean">
      <p>🥔</p>
      <div class="popup-soybean">
        <h3>👈</h3>
        <h6><%= soybean.classification %></h6>

        <% @data_structure.each_with_index do |data, i| %>
          <% next if i == 0  %>
          <% if soybean[data.second[:column].to_sym] != 'absent' %>
            <p>
              <span class='popup-label'>
                <%= data.second[:column] %>:
              </span>
              <%= soybean[data.second[:column].to_sym] %>
            </p>
          <% end %>
        <% end %>

      </div>
    </div>
  <% end %>
</div>
