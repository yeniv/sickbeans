<h1>Soybeans</h1>

<%= simple_form_for :search, url: root_path, method: 'GET' do |f| %>
  <%=  %>
  <%= f.input :classifications, collection: @classifications, as: :check_boxes %>
  <%= f.button :submit %>
<% end %>

<h3>Count: <%= @soybeans.count %></h3>

<% @soybeans.each do |soybean| %>
    <p>🥔 <%= soybean.classification %></p>
<% end %>
